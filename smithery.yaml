runtime: "typescript"

# This is your TypeScript entry point.
entry: "src/index.ts"

# The default build and launch configuration Smithery Cloud will follow.
build:
  command: ["npm", "run", "build"]
  output: "dist"

start:
  command: ["node", "dist/index.js"]
  port: 3010
